{
    "tasks": [
        {
            "type": "shell",
            "label": "g++ Build main",
            "command": "/usr/bin/g++",
            "args": [
                "-g",
                "main.cpp",
                "-o",
                "build/main.out"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Custom made, builds main.cpp, should be update when adding more classes."
        }
    ],
    "version": "2.0.0"
}